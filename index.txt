обьяснение html кода
<!doctype html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
        content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Shop</title>
Дефолт код всегда 1 кроме слово шоп там про что у нас веб приложение будет

стили
    <style>
        /* Цвета */
        body {
            background: #e9e9e9;
            font-family: Arial;
            margin: 40px 20px;
        }

        /* Карточка товара */
        #main {
            background: white;
            max-width: 350px;
            margin: 0 auto;
            padding: 25px;
            border-radius: 15px;
            text-align: center;
        }

        /* Заголовок */
        h1 {
            color: black;
            font-size: 28px;
            margin-top: 0;
        }

        /* Картинка */
        img {
            width: 120px;
            height: 120px;
        }

        /* Описание */
        p {
            color: gray;
            font-size: 15px;
            line-height: 1.5;
            margin: 20px 0;
        }

        /* Кнопка */
        #buy {
            background: blue;
            color: white;
            font-size: 18px;
            padding: 12px 35px;
            border: none;
            border-radius: 8px;
        }

        /* Эффект при наведении */
        #buy:hover {
            background: darkblue;
        }

        /* стиль дня кнопок номер емаел имя*/
        #form{
            display: none;
            text-align: center;
        }

        input{
            width: 90%;
            outline: none;
            margin: 10px 5%;
            padding: 15px 10px;
            font-size: 14px;
            border: 2px solid silver;
            border-radius: 5px;
        }

        input:focus{
            border-color: #db5d5d;
        }
    </style>

сам визуал веб сайта
</head>
<body>
    <div id="main">
        <h1>Онлайн магазин</h1> название
        <img src="https://cdn-icons-png.flaticon.com/512/3595/3595455.png"> сылка на фотку на фоне
        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusantium ipsum magni, molestias.</p> что будет писать снизу
        <button id="buy">Купить</button> баттон это кнопка
    </div>

форма
    <form id=""from>
        <input type="text" placeholder="Имя" id="user_name">
        <input type="text" placeholder="Email" id="user_email">
        <input type="text" placeholder="Телефон" id="user_phone">
        <div id = "error"></div> обьект ерор для ошибок
        <button id="order">Оформить</button>
    </form>

скрипт
    <script src="https://telegram.org/js/telegram-web-app.js"></script> для работы с тг
    <script>
        let tg = window.Telegram.WebApp; для работы с тг
        let buy = document.getElementById("buy");  переменая ищущая кнопку bau order
        let order = document.getElementById("order");

        обработчик событие тоесть при клике будет вызывать анонимную функцию
        buy.addEventListener("click", ()=> {
                            document.getElementById("error").innerText = " ";     если заново нажимаем на кнопку убираем ошибку и снова вводим данные
            document.getElementById("main").style.display="none"; будет прятаться
            document.getElementById("form").style.display="block"; буду показывать другой блок тоесть блок форм
            document.getElementById("user_name").value = tg.initDaraUnsafe.user.first_name + " " + tg.initDaraUnsafe.user.last_name;
                        обратимс к элементу юзер нэйм к качестве значение будет получать от апи тг имя и фамилию 
                        при кнопке купить будет в одно поле будет пождставлять данные пользователя 
                        все значени есть в документации для css html
        })

        обработчик событие аноним функции будет закрываться окошко после кнопки оформить
        order.addEventListener("click", () => {
            th.close();
        })
    </script>